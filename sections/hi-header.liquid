{% doc %}
  Hi Header — sticky nav with 3-level mega menus and mobile drawer accordion.

  Nav items are section blocks. Each block can optionally have a mega_linklist:
    - Top-level links in the linklist = column headers (level 2)
    - Children of those links = items in each column (level 3)
  Featured image, title, and CTA are block settings for the mega panel right panel.
{% enddoc %}

<header class="hi-header" data-hi-header>
  <div class="hi-header-inner">

    {%- comment -%}──── LEFT: burger + desktop nav ────{%- endcomment -%}
    <div class="hi-header-left">

      <button type="button" class="hi-burger" data-hi-burger
        aria-label="{{ 'hi.general.open_menu' | t }}"
        aria-expanded="false"
        aria-controls="hi-drawer">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
          <line x1="3" y1="6" x2="21" y2="6"></line>
          <line x1="3" y1="12" x2="21" y2="12"></line>
          <line x1="3" y1="18" x2="21" y2="18"></line>
        </svg>
      </button>

      <nav class="hi-desktop-nav" aria-label="{{ 'hi.general.main_nav' | t }}">
        {%- for block in section.blocks -%}
          {%- assign mega_list = linklists[block.settings.mega_linklist] -%}
          {%- assign has_mega = false -%}
          {%- if mega_list.links.size > 0 -%}{%- assign has_mega = true -%}{%- endif -%}

          <div class="hi-navitem" {{ block.shopify_attributes }}>
            <a href="{{ block.settings.url | default: '#' }}"
              class="hi-navlink"
              {%- if block.settings.url == request.path %} aria-current="page"{%- endif -%}>
              {{ block.settings.label }}
            </a>

            {%- if has_mega -%}
              <div class="hi-megapanel" role="region" aria-label="{{ block.settings.label }}">
                <div class="hi-megapanel-inner">

                  {%- comment -%} Column headers (level 2) + their child links (level 3) {%- endcomment -%}
                  <div class="hi-megacols">
                    {%- for col in mega_list.links -%}
                      <div class="hi-megacol">
                        <div class="hi-megacol-title">{{ col.title }}</div>
                        {%- for child in col.links -%}
                          <a href="{{ child.url }}" class="hi-megalink">{{ child.title }}</a>
                        {%- endfor -%}
                      </div>
                    {%- endfor -%}
                  </div>

                  {%- comment -%} Featured image card (right panel) {%- endcomment -%}
                  {%- if block.settings.mega_image != blank or block.settings.mega_title != blank -%}
                    <a href="{{ block.settings.mega_cta_url | default: block.settings.url }}" class="hi-megafeat">
                      <div class="hi-megafeat-img">
                        {%- if block.settings.mega_image != blank -%}
                          {{ block.settings.mega_image | image_url: width: 700 | image_tag:
                              loading: 'lazy',
                              alt: block.settings.mega_title,
                              class: 'hi-megafeat-photo' }}
                        {%- endif -%}
                      </div>
                      <div class="hi-megafeat-grad"></div>
                      <div class="hi-megafeat-content">
                        <div class="hi-megafeat-label">{{ block.settings.label }}</div>
                        {%- if block.settings.mega_title != blank -%}
                          <div class="hi-megafeat-title">{{ block.settings.mega_title }}</div>
                        {%- endif -%}
                        <div class="hi-megafeat-cta">{{ block.settings.mega_cta_text | default: 'Shop Now' }} →</div>
                      </div>
                    </a>
                  {%- endif -%}

                </div>
              </div>
            {%- endif -%}
          </div>
        {%- endfor -%}
      </nav>
    </div>

    {%- comment -%}──── CENTER: logo ────{%- endcomment -%}
    <a href="{{ routes.root_url }}" class="hi-logo-link" aria-label="{{ shop.name }}">
      {%- if section.settings.logo != blank -%}
        {{ section.settings.logo | image_url: width: 200 | image_tag:
            class: 'hi-logo-img', loading: 'eager', alt: shop.name }}
      {%- else -%}
        <div class="hi-logo-text">
          <span class="hi-logo-l1">{{ section.settings.logo_text_line1 | default: 'HONOR IN' }}</span>
          <span class="hi-logo-l2">{{ section.settings.logo_text_line2 | default: 'CLOTHING CO.' }}</span>
        </div>
      {%- endif -%}
    </a>

    {%- comment -%}──── RIGHT: search, account, cart ────{%- endcomment -%}
    <div class="hi-header-right">
      <a href="{{ routes.search_url }}" class="hi-navlink hi-desktop-only">{{ 'hi.general.search' | t }}</a>
      {%- if shop.customer_accounts_enabled -%}
        <a href="{{ routes.account_url }}" class="hi-navlink hi-desktop-only">{{ 'hi.general.account' | t }}</a>
      {%- endif -%}
      <button type="button" class="hi-cart-trigger" data-hi-cart-trigger
        aria-label="{{ 'hi.general.cart' | t }}">
        {{ 'hi.general.cart' | t }}
        <span class="hi-cart-badge" data-cart-count>{{ cart.item_count }}</span>
      </button>
    </div>

  </div>
</header>

{%- comment -%}──── MOBILE DRAWER (slide-in from left) ────{%- endcomment -%}
<div class="hi-drawer-backdrop" data-hi-drawer-backdrop></div>
<aside class="hi-drawer" id="hi-drawer" data-hi-drawer
  role="dialog" aria-modal="true" aria-label="{{ 'hi.general.menu' | t }}">

  <div class="hi-drawer-head">
    <div class="hi-logo-text">
      <span class="hi-logo-l1" style="font-size:18px;letter-spacing:.16em;">
        {{ section.settings.logo_text_line1 | default: 'HONOR IN' }}
      </span>
    </div>
    <button type="button" class="hi-drawer-close" data-hi-drawer-close
      aria-label="{{ 'hi.general.close_menu' | t }}">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
        <line x1="6" y1="6" x2="18" y2="18"></line>
        <line x1="18" y1="6" x2="6" y2="18"></line>
      </svg>
    </button>
  </div>

  <a href="{{ routes.search_url }}" class="hi-drawer-row">{{ 'hi.general.search' | t }}</a>

  {%- for block in section.blocks -%}
    {%- assign mega_list = linklists[block.settings.mega_linklist] -%}
    {%- if mega_list.links.size > 0 -%}
      {%- comment -%} Accordion row with sub-columns {%- endcomment -%}
      <button type="button" class="hi-drawer-row" data-hi-acc aria-expanded="false">
        {{ block.settings.label }}
        <span class="hi-acc-plus" aria-hidden="true">+</span>
      </button>
      <div class="hi-acc-panel" role="region">
        {%- for col in mega_list.links -%}
          <div class="hi-acc-coltitle">{{ col.title }}</div>
          {%- for child in col.links -%}
            <a href="{{ child.url }}" class="hi-acc-link">{{ child.title }}</a>
          {%- endfor -%}
        {%- endfor -%}
      </div>
    {%- else -%}
      <a href="{{ block.settings.url | default: '#' }}" class="hi-drawer-row">
        {{ block.settings.label }}
      </a>
    {%- endif -%}
  {%- endfor -%}

  {%- if shop.customer_accounts_enabled -%}
    <a href="{{ routes.account_url }}" class="hi-drawer-row">{{ 'hi.general.account' | t }}</a>
  {%- endif -%}

  <button type="button" class="hi-drawer-row" data-hi-cart-trigger>
    {{ 'hi.general.cart' | t }} ({{ cart.item_count }})
  </button>
</aside>

{% stylesheet %}
/* ─────────── Header shell ─────────── */
.hi-header {
  position: sticky;
  top: 0;
  z-index: 50;
  background: rgba(251,249,245,.96);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border-bottom: 1px solid #E7E2D7;
  transition: transform .35s cubic-bezier(.2,.7,.3,1);
  will-change: transform;
}
.hi-header-inner {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 32px;
  height: 74px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
}

/* ─────────── Left ─────────── */
.hi-header-left {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 18px;
  min-width: 0;
}

/* ─────────── Burger (mobile-only) ─────────── */
.hi-burger {
  display: none !important;
  background: none;
  border: none;
  cursor: pointer;
  padding: 6px;
  margin-left: -6px;
  color: #16150F;
  align-items: center;
}
.hi-burger svg { width: 24px; height: 24px; display: block; }

/* ─────────── Desktop nav ─────────── */
.hi-desktop-nav {
  display: flex;
  align-items: center;
  gap: 26px;
}
/* hi-navitem must NOT have position:relative — megapanel positions vs. header */
.hi-navitem {
  display: flex;
  align-items: center;
  height: 74px;
}

/* ─────────── Nav links with animated underline ─────────── */
.hi-navlink {
  position: relative;
  color: #3A372F;
  font-family: 'Oswald', sans-serif;
  font-weight: 500;
  font-size: 13px;
  letter-spacing: .13em;
  text-transform: uppercase;
  text-decoration: none;
  cursor: pointer;
  white-space: nowrap;
  transition: color .2s cubic-bezier(.2,.7,.3,1);
}
.hi-navlink::after {
  content: '';
  position: absolute;
  left: 0; right: 0;
  bottom: -4px;
  height: 2px;
  background: var(--hi-accent, #5C6B47);
  transform: scaleX(0);
  transform-origin: left;
  transition: transform .26s cubic-bezier(.2,.7,.3,1);
}
.hi-navitem:hover > .hi-navlink,
.hi-navlink:hover { color: #16150F; }
.hi-navitem:hover > .hi-navlink::after,
.hi-navlink:hover::after { transform: scaleX(1); }

/* ─────────── Mega panel (3-level dropdown) ─────────── */
.hi-megapanel {
  position: absolute;
  top: 100%;
  left: 0;
  width: 100vw;
  background: #FBF9F5;
  border-top: 1px solid #E7E2D7;
  box-shadow: 0 30px 46px rgba(20,21,15,.13);
  opacity: 0;
  visibility: hidden;
  transform: translateY(-10px);
  pointer-events: none;
  transition: opacity .26s cubic-bezier(.2,.7,.3,1),
              transform .26s cubic-bezier(.2,.7,.3,1),
              visibility .26s;
  z-index: 45;
}
.hi-navitem:hover .hi-megapanel {
  opacity: 1;
  visibility: visible;
  transform: none;
  pointer-events: auto;
}
.hi-megapanel-inner {
  max-width: 1280px;
  margin: 0 auto;
  padding: 42px 32px 46px;
  display: flex;
  gap: 56px;
  align-items: flex-start;
}
/* Columns wrapper (level 2 + 3) */
.hi-megacols {
  display: flex;
  gap: 56px;
  flex: 1;
  flex-wrap: wrap;
}
.hi-megacol { min-width: 100px; }
/* Level 2: column header */
.hi-megacol-title {
  font-family: 'Oswald', sans-serif;
  font-weight: 600;
  font-size: 12px;
  letter-spacing: .18em;
  text-transform: uppercase;
  color: #16150F;
  margin-bottom: 14px;
}
/* Level 3: column items */
.hi-megalink {
  display: block;
  color: #54504A;
  text-decoration: none;
  font-size: 14.5px;
  padding: 7px 0;
  width: max-content;
  transition: color .2s ease, transform .2s ease;
}
.hi-megalink:hover { color: #16150F; transform: translateX(5px); }

/* ─────────── Mega featured image (right panel) ─────────── */
.hi-megafeat {
  position: relative;
  display: block;
  width: 300px;
  flex-shrink: 0;
  aspect-ratio: 5 / 4;
  overflow: hidden;
  text-decoration: none;
  background: #E9E4DA;
  transition: box-shadow .4s cubic-bezier(.2,.7,.3,1);
}
.hi-megafeat:hover { box-shadow: 0 18px 38px rgba(20,21,15,.2); }
.hi-megafeat-img { position: absolute; inset: 0; overflow: hidden; }
.hi-megafeat-photo {
  width: 100%; height: 100%;
  object-fit: cover; display: block;
  transition: transform .9s cubic-bezier(.2,.7,.3,1);
}
.hi-megafeat:hover .hi-megafeat-photo { transform: scale(1.07); }
.hi-megafeat-grad {
  position: absolute; inset: 0;
  background: linear-gradient(180deg, rgba(12,12,9,0) 28%, rgba(12,12,9,.74) 100%);
}
.hi-megafeat-content {
  position: absolute; left: 0; right: 0; bottom: 0;
  padding: 22px 24px; color: #fff;
}
.hi-megafeat-label {
  font-family: 'Oswald', sans-serif;
  font-weight: 500; font-size: 11px;
  letter-spacing: .2em; text-transform: uppercase;
  color: #E2DCCE; margin-bottom: 6px;
}
.hi-megafeat-title {
  font-family: 'Oswald', sans-serif;
  font-weight: 600; font-size: 20px;
  text-transform: uppercase; letter-spacing: .02em; line-height: 1.05;
}
.hi-megafeat-cta {
  font-family: 'Oswald', sans-serif;
  font-weight: 600; font-size: 12px;
  letter-spacing: .14em; text-transform: uppercase;
  margin-top: 12px;
  border-bottom: 2px solid var(--hi-accent, #5C6B47);
  padding-bottom: 3px; display: inline-block;
}

/* ─────────── Logo ─────────── */
.hi-logo-link {
  text-decoration: none;
  text-align: center;
  flex-shrink: 0;
  transition: opacity .2s ease;
}
.hi-logo-link:hover { opacity: .7; }
.hi-logo-img { height: 40px; width: auto; display: block; }
.hi-logo-text { display: flex; flex-direction: column; align-items: center; }
.hi-logo-l1 {
  font-family: 'Oswald', sans-serif;
  font-weight: 700; font-size: 23px;
  letter-spacing: .18em; color: #16150F; line-height: 1;
}
.hi-logo-l2 {
  font-family: 'Oswald', sans-serif;
  font-weight: 400; font-size: 9.5px;
  letter-spacing: .42em; color: var(--hi-accent, #5C6B47); margin-top: 3px;
}

/* ─────────── Right: text links + cart ─────────── */
.hi-header-right {
  display: flex;
  align-items: center;
  gap: 22px;
  flex: 1;
  justify-content: flex-end;
}
.hi-cart-trigger {
  display: flex;
  align-items: center;
  gap: 7px;
  background: none; border: none; padding: 0;
  cursor: pointer;
  color: #16150F;
  font-family: 'Oswald', sans-serif;
  font-weight: 600; font-size: 13px;
  letter-spacing: .13em; text-transform: uppercase;
  transition: opacity .2s ease;
}
.hi-cart-trigger:hover { opacity: .6; }
.hi-cart-badge {
  background: var(--hi-accent, #5C6B47);
  color: #fff;
  border-radius: 50%;
  min-width: 18px; height: 18px;
  padding: 0 5px;
  display: inline-flex; align-items: center; justify-content: center;
  font-family: 'Oswald', sans-serif;
  font-size: 11px; font-weight: 600; line-height: 1;
}

/* ─────────── Mobile drawer ─────────── */
.hi-drawer-backdrop {
  position: fixed; inset: 0;
  background: rgba(12,12,9,.5);
  opacity: 0; visibility: hidden;
  transition: opacity .3s ease, visibility .3s;
  z-index: 199;
}
.hi-drawer-backdrop[data-open="true"] { opacity: 1; visibility: visible; }

.hi-drawer {
  position: fixed; top: 0; left: 0; bottom: 0;
  width: 86%; max-width: 360px;
  background: #FBF9F5;
  z-index: 200;
  transform: translateX(-100%);
  transition: transform .34s cubic-bezier(.2,.7,.3,1);
  overflow-y: auto; -webkit-overflow-scrolling: touch;
}
.hi-drawer[data-open="true"] {
  transform: none;
  box-shadow: 0 0 60px rgba(12,12,9,.34);
}
.hi-drawer-head {
  display: flex; align-items: center; justify-content: space-between;
  padding: 20px 22px;
  border-bottom: 1px solid #E7E2D7;
  position: sticky; top: 0; background: #FBF9F5;
}
.hi-drawer-close {
  background: none; border: none;
  cursor: pointer; color: #16150F;
  padding: 4px; line-height: 0;
}
.hi-drawer-close svg { width: 22px; height: 22px; }

.hi-drawer-row {
  display: flex; align-items: center; justify-content: space-between;
  gap: 12px; width: 100%;
  padding: 16px 22px;
  border: none; border-bottom: 1px solid #ECE7DC;
  background: none; cursor: pointer;
  font-family: 'Oswald', sans-serif;
  font-weight: 600; font-size: 16px;
  letter-spacing: .08em; text-transform: uppercase;
  color: #16150F; text-decoration: none; text-align: left;
  box-sizing: border-box;
}
.hi-acc-plus {
  font-size: 22px; font-weight: 300; line-height: 1;
  transition: transform .3s ease;
  color: var(--hi-accent, #5C6B47);
}
.hi-drawer-row[data-open="true"] .hi-acc-plus { transform: rotate(45deg); }
.hi-acc-panel {
  max-height: 0; overflow: hidden;
  transition: max-height .34s cubic-bezier(.2,.7,.3,1);
  background: #F1ECE1;
}
.hi-acc-panel[data-open="true"] { max-height: 1200px; }
.hi-acc-coltitle {
  font-family: 'Oswald', sans-serif;
  font-weight: 600; font-size: 11px;
  letter-spacing: .18em; text-transform: uppercase;
  color: #8A8479; padding: 16px 26px 6px;
}
.hi-acc-link {
  display: block; padding: 9px 26px;
  color: #54504A; text-decoration: none;
  font-size: 15px;
  transition: color .2s ease;
}
.hi-acc-link:hover { color: #16150F; }

/* ─────────── Responsive ─────────── */
@media (max-width: 900px) {
  .hi-desktop-nav { display: none !important; }
  .hi-desktop-only { display: none !important; }
  .hi-burger { display: inline-flex !important; }
  .hi-header-inner { padding: 0 20px; }
}
{% endstylesheet %}

{% javascript %}
(function () {
  'use strict';

  /* ── Drawer open / close ── */
  var burger   = document.querySelector('[data-hi-burger]');
  var backdrop = document.querySelector('[data-hi-drawer-backdrop]');
  var drawer   = document.querySelector('[data-hi-drawer]');
  var closeBtn = document.querySelector('[data-hi-drawer-close]');

  function openDrawer() {
    if (!drawer) return;
    drawer.setAttribute('data-open', 'true');
    if (backdrop) backdrop.setAttribute('data-open', 'true');
    if (burger) burger.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }
  function closeDrawer() {
    if (!drawer) return;
    drawer.removeAttribute('data-open');
    if (backdrop) backdrop.removeAttribute('data-open');
    if (burger) burger.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }

  if (burger)   burger.addEventListener('click', openDrawer);
  if (backdrop) backdrop.addEventListener('click', closeDrawer);
  if (closeBtn) closeBtn.addEventListener('click', closeDrawer);

  /* Close drawer when an anchor link inside is tapped */
  if (drawer) {
    drawer.querySelectorAll('a').forEach(function (a) {
      a.addEventListener('click', closeDrawer);
    });
  }

  /* ── Accordion rows in mobile drawer ── */
  document.querySelectorAll('[data-hi-acc]').forEach(function (btn) {
    btn.addEventListener('click', function () {
      var isOpen = btn.getAttribute('data-open') === 'true';
      var panel  = btn.nextElementSibling;
      btn.setAttribute('data-open',      isOpen ? 'false' : 'true');
      btn.setAttribute('aria-expanded',  isOpen ? 'false' : 'true');
      if (panel) panel.setAttribute('data-open', isOpen ? 'false' : 'true');
    });
  });

  /* ── Scroll hide / show header ── */
  var header = document.querySelector('[data-hi-header]');
  if (header) {
    var lastY = 0, ticking = false;
    window.addEventListener('scroll', function () {
      if (!ticking) {
        requestAnimationFrame(function () {
          var y = window.scrollY || 0;
          header.style.transform = (y > lastY && y > 140)
            ? 'translateY(-100%)'
            : 'translateY(0)';
          lastY = y;
          ticking = false;
        });
        ticking = true;
      }
    }, { passive: true });
  }
})();
{% endjavascript %}

{% schema %}
{
  "name": "Header",
  "blocks": [
    {
      "type": "nav_item",
      "name": "Navigation item",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Shop"
        },
        {
          "type": "url",
          "id": "url",
          "label": "Link URL"
        },
        {
          "type": "header",
          "content": "Mega Menu (optional)"
        },
        {
          "type": "paragraph",
          "content": "Create a navigation menu in Shopify Admin → Navigation. Top-level links become column headers; their child links become the items inside each column."
        },
        {
          "type": "link_list",
          "id": "mega_linklist",
          "label": "Mega menu navigation"
        },
        {
          "type": "image_picker",
          "id": "mega_image",
          "label": "Featured image"
        },
        {
          "type": "text",
          "id": "mega_title",
          "label": "Featured title"
        },
        {
          "type": "text",
          "id": "mega_cta_text",
          "label": "CTA text",
          "default": "Shop Now"
        },
        {
          "type": "url",
          "id": "mega_cta_url",
          "label": "CTA URL"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "header",
      "content": "Logo"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo image (overrides text logo)"
    },
    {
      "type": "text",
      "id": "logo_text_line1",
      "label": "Logo line 1",
      "default": "HONOR IN"
    },
    {
      "type": "text",
      "id": "logo_text_line2",
      "label": "Logo line 2",
      "default": "CLOTHING CO."
    }
  ],
  "presets": [
    {
      "name": "Header",
      "blocks": [
        {
          "type": "nav_item",
          "settings": { "label": "New", "url": "/collections/new-arrivals" }
        },
        {
          "type": "nav_item",
          "settings": { "label": "Men", "url": "/collections/mens" }
        },
        {
          "type": "nav_item",
          "settings": { "label": "Women", "url": "/collections/womens" }
        },
        {
          "type": "nav_item",
          "settings": { "label": "Youth", "url": "/collections/youth" }
        },
        {
          "type": "nav_item",
          "settings": { "label": "Shop by Activity", "url": "/collections/activity" }
        },
        {
          "type": "nav_item",
          "settings": { "label": "About", "url": "/pages/about" }
        }
      ]
    }
  ]
}
{% endschema %}
