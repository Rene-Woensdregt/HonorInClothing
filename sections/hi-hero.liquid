<section class="hi-hero">
  {%- if section.settings.image != blank -%}
    <div class="hi-hero-bg" aria-hidden="true">
      {{ section.settings.image | image_url: width: 1800 | image_tag: class: 'hi-hero-bg-img', loading: 'eager', alt: section.settings.heading, fetchpriority: 'high' }}
    </div>
  {%- else -%}
    <div class="hi-hero-bg hi-hero-bg--placeholder" aria-hidden="true"></div>
  {%- endif -%}
  <div class="hi-hero-overlay" aria-hidden="true"></div>
  <div class="hi-hero-content" data-hi-reveal>
    {%- if section.settings.eyebrow != blank -%}
      <div class="hi-hero-eyebrow">
        <span class="hi-hero-eyebrow-line" aria-hidden="true"></span>
        {{ section.settings.eyebrow }}
      </div>
    {%- endif -%}
    {%- if section.settings.heading != blank -%}
      <h1 class="hi-hero-heading">{{ section.settings.heading | newline_to_br }}</h1>
    {%- endif -%}
    {%- if section.settings.subheading != blank -%}
      <p class="hi-hero-sub">{{ section.settings.subheading }}</p>
    {%- endif -%}
    <div class="hi-hero-btns">
      {%- if section.settings.cta_label != blank -%}
        <a href="{{ section.settings.cta_url }}" class="hi-btn hi-btn--light">{{ section.settings.cta_label }}</a>
      {%- endif -%}
      {%- if section.settings.cta2_label != blank -%}
        <a href="{{ section.settings.cta2_url }}" class="hi-btn hi-btn--ghost">{{ section.settings.cta2_label }}</a>
      {%- endif -%}
    </div>
  </div>
</section>

{% stylesheet %}
.hi-hero {
  position: relative;
  height: 90vh;
  min-height: 600px;
  max-height: 880px;
  overflow: hidden;
  display: flex;
  align-items: flex-end;
}
.hi-hero-bg {
  position: absolute;
  inset: 0;
  overflow: hidden;
}
.hi-hero-bg-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  animation: hi-kenburns 14s ease-out both;
}
.hi-hero-bg--placeholder {
  background: #2a2a22;
}
.hi-hero-overlay {
  display: block !important;
  position: absolute;
  inset: 0;
  background: linear-gradient(
    177deg,
    rgba(12,12,9,.30) 0%,
    rgba(12,12,9,.12) 38%,
    rgba(12,12,9,.82) 100%
  );
  pointer-events: none;
}
.hi-hero-content {
  position: relative;
  z-index: 2;
  max-width: 1280px;
  width: 100%;
  margin: 0 auto;
  padding: 0 32px 78px;
  color: #fff;
  text-shadow: 0 2px 22px rgba(10,11,8,.42);
}
.hi-hero-eyebrow {
  display: flex;
  align-items: center;
  gap: 14px;
  font-family: 'Oswald', sans-serif;
  font-size: 13px;
  font-weight: 600;
  letter-spacing: .28em;
  text-transform: uppercase;
  color: #E9E3D4;
  margin-bottom: 22px;
}
.hi-hero-eyebrow-line {
  display: inline-block;
  width: 34px;
  height: 1px;
  background: #5C6B47;
  flex-shrink: 0;
}
.hi-hero-heading {
  font-family: 'Oswald', sans-serif;
  font-weight: 700;
  font-size: clamp(42px, 6.6vw, 88px);
  letter-spacing: .005em;
  text-transform: uppercase;
  line-height: .96;
  margin: 0;
  max-width: 15ch;
  text-wrap: balance;
  color: #fff;
}
.hi-hero .hi-hero-sub {
  font-family: 'Hanken Grotesk', sans-serif;
  font-size: 18px;
  font-weight: 400;
  line-height: 1.65;
  color: rgba(247,243,234,.88);
  max-width: 50ch;
  margin: 24px 0 34px;
}
.hi-hero-btns {
  display: flex;
  flex-wrap: wrap;
  gap: 14px;
}
/* Ghost button on dark hero needs white treatment for visibility */
.hi-hero .hi-btn--ghost {
  color: rgba(255,255,255,.82) !important;
  border-color: rgba(255,255,255,.45) !important;
}
.hi-hero .hi-btn--ghost:hover {
  background: rgba(255,255,255,.14) !important;
  border-color: rgba(255,255,255,.75) !important;
  color: #fff !important;
}
@media (max-width: 900px) {
  .hi-hero {
    height: 82vh;
    min-height: 540px;
  }
  .hi-hero-content {
    padding: 0 24px 52px;
  }
  .hi-hero-eyebrow {
    font-size: 11px;
    letter-spacing: .12em;
    margin-bottom: 14px;
    gap: 10px;
  }
  .hi-hero-heading {
    font-size: clamp(34px, 9vw, 46px);
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "Hero Banner",
  "settings": [
    {
      "type": "header",
      "content": "Background"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Background image",
      "info": "Recommended: 1800×1200px or wider. Landscape photos work best."
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "placeholder": "e.g. Premium Active Lifestyle Apparel",
      "default": "Premium Active Lifestyle Apparel",
      "info": "Small uppercase label shown above the heading with a sage green line"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading",
      "placeholder": "e.g. Live With Honor.",
      "default": "Live With Honor.",
      "info": "Press Enter to split the heading across multiple lines"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "placeholder": "e.g. Built for those who show up.",
      "default": "Built for those who show up."
    },
    {
      "type": "header",
      "content": "Primary button"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Button label",
      "placeholder": "e.g. Shop New Arrivals",
      "default": "Shop New Arrivals",
      "info": "Leave blank to hide this button"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Button URL"
    },
    {
      "type": "header",
      "content": "Secondary button"
    },
    {
      "type": "text",
      "id": "cta2_label",
      "label": "Button label",
      "placeholder": "e.g. Our Story",
      "default": "Our Story",
      "info": "Leave blank to hide this button. Renders as an outline (ghost) style."
    },
    {
      "type": "url",
      "id": "cta2_url",
      "label": "Button URL"
    }
  ],
  "presets": [
    {
      "name": "Hero Banner"
    }
  ]
}
{% endschema %}
