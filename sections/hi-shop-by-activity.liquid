<section class="hi-act-section">
  <div class="hi-act-wrap">
    <div class="hi-act-header" data-hi-reveal>
      {%- if section.settings.eyebrow != blank -%}
        <div class="hi-act-eyebrow">{{ section.settings.eyebrow }}</div>
      {%- endif -%}
      {%- if section.settings.title != blank -%}
        <h2 class="hi-act-title">{{ section.settings.title }}</h2>
      {%- endif -%}
    </div>
    <div class="hi-act-grid">
      {%- for i in (1..3) -%}
        {%- liquid
          assign label_key = 'activity_' | append: i | append: '_label'
          assign sublabel_key = 'activity_' | append: i | append: '_sublabel'
          assign image_key = 'activity_' | append: i | append: '_image'
          assign url_key = 'activity_' | append: i | append: '_url'
          assign label = section.settings[label_key]
          assign sublabel = section.settings[sublabel_key]
          assign image = section.settings[image_key]
          assign url = section.settings[url_key]
        -%}
        <a href="{{ url }}" class="hi-tile hi-act-tile">
          {%- if image != blank -%}
            {{ image | image_url: width: 800 | image_tag: class: 'hi-card-img', loading: 'lazy', alt: label }}
          {%- else -%}
            <div class="hi-act-placeholder"></div>
          {%- endif -%}
          <div class="hi-act-tile-overlay" aria-hidden="true"></div>
          <div class="hi-act-tile-text">
            {%- if sublabel != blank -%}
              <div class="hi-act-tile-sublabel">{{ sublabel }}</div>
            {%- endif -%}
            <div class="hi-act-tile-main">
              <span class="hi-act-tile-label">{{ label }}</span>
              <span class="hi-tile-arrow" aria-hidden="true">→</span>
            </div>
          </div>
        </a>
      {%- endfor -%}
    </div>
  </div>
</section>

{% stylesheet %}
.hi-act-section { padding: 80px 32px; background: #FBF9F5; }
.hi-act-wrap { max-width: 1280px; margin: 0 auto; }
.hi-act-header { text-align: center; margin-bottom: 40px; }
.hi-act-eyebrow {
  font-family: 'Oswald', sans-serif;
  font-size: 13px;
  font-weight: 600;
  letter-spacing: .24em;
  text-transform: uppercase;
  color: #5C6B47;
  margin-bottom: 12px;
}
.hi-act-title {
  font-family: 'Oswald', sans-serif;
  font-size: clamp(28px, 3vw, 46px);
  font-weight: 600;
  letter-spacing: .01em;
  text-transform: uppercase;
  color: #16150F;
  margin: 0;
}
.hi-act-tile {
  display: block;
  position: relative;
  overflow: hidden;
  aspect-ratio: 4 / 5;
  text-decoration: none;
}
.hi-card-img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform .9s cubic-bezier(.2,.7,.3,1);
}
.hi-act-tile:hover .hi-card-img { transform: scale(1.07); }
.hi-act-placeholder {
  position: absolute;
  inset: 0;
  background: #C5BFB5;
}
.hi-act-tile-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(12,12,9,0) 40%, rgba(12,12,9,.72) 100%);
  transition: opacity .3s;
}
.hi-act-tile:hover .hi-act-tile-overlay { opacity: .85; }
.hi-act-tile-text {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  display: block;
  padding: 30px 28px;
  z-index: 2;
}
.hi-act-tile-sublabel {
  font-family: 'Oswald', sans-serif;
  font-size: 11.5px;
  font-weight: 500;
  letter-spacing: .2em;
  text-transform: uppercase;
  color: #E2DCCE;
  margin-bottom: 4px;
}
.hi-act-tile-main {
  display: flex;
  align-items: center;
  gap: 12px;
}
.hi-act-tile-label {
  font-family: 'Oswald', sans-serif;
  font-size: 27px;
  font-weight: 600;
  letter-spacing: .02em;
  text-transform: uppercase;
  color: #fff;
}
.hi-tile-arrow {
  font-size: 18px;
  font-weight: 600;
  color: #fff;
}
@media (max-width: 900px) {
  .hi-act-section { padding: 56px 20px; }
  .hi-act-grid { grid-template-columns: 1fr !important; }
  .hi-act-tile { aspect-ratio: 16 / 9; }
}
{% endstylesheet %}

{% schema %}
{
  "name": "Shop by Activity",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Find Your Fit"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Shop By Activity"
    },
    {
      "type": "text",
      "id": "activity_1_sublabel",
      "label": "Activity 1 sub-label",
      "default": "Hiking & Outdoors"
    },
    {
      "type": "text",
      "id": "activity_1_label",
      "label": "Activity 1 title",
      "default": "On The Trail"
    },
    {
      "type": "image_picker",
      "id": "activity_1_image",
      "label": "Activity 1 image"
    },
    {
      "type": "url",
      "id": "activity_1_url",
      "label": "Activity 1 URL"
    },
    {
      "type": "text",
      "id": "activity_2_sublabel",
      "label": "Activity 2 sub-label",
      "default": "Fitness & Performance"
    },
    {
      "type": "text",
      "id": "activity_2_label",
      "label": "Activity 2 title",
      "default": "In Training"
    },
    {
      "type": "image_picker",
      "id": "activity_2_image",
      "label": "Activity 2 image"
    },
    {
      "type": "url",
      "id": "activity_2_url",
      "label": "Activity 2 URL"
    },
    {
      "type": "text",
      "id": "activity_3_sublabel",
      "label": "Activity 3 sub-label",
      "default": "Work & Life"
    },
    {
      "type": "text",
      "id": "activity_3_label",
      "label": "Activity 3 title",
      "default": "Everyday Carry"
    },
    {
      "type": "image_picker",
      "id": "activity_3_image",
      "label": "Activity 3 image"
    },
    {
      "type": "url",
      "id": "activity_3_url",
      "label": "Activity 3 URL"
    }
  ],
  "presets": [
    {
      "name": "Shop by Activity"
    }
  ]
}
{% endschema %}
